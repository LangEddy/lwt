# Copy to frontend/.env and adjust if needed.
#
# These defaults match the local Supabase stack started by `supabase start`.
# The anon key below is the well-known fixed key the local CLI uses for
# every project (it is NOT a secret and never grants access to anything
# outside your local Docker stack). To confirm, run `supabase status`.

VITE_API_URL=http://localhost:3000
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
